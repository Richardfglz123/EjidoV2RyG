[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "mysql80"]

[phases.install]
dependsOn = ["setup"]
cmds = ["composer install --ignore-platform-reqs --no-interaction"]

[variables]
PHP_EXTENSION_INSTALLER = "pdo_mysql,mysqli,mbstring,gd,xml,zip"
NIXPACKS_PHP_FALLBACK_BINARY = "php82"